import type { PublicSettings } from "./webviewApiTypes";
/**
 * Get public settings
 * Returns privacy-safe subset of settings (for Pera Connect)
 */
export declare function getPublicSettings(timeoutMs?: number): Promise<PublicSettings | null>;
