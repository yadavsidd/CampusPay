import { BoxReference } from './types/transactions/base.js';
/**
 * boxReferencesToEncodingData translates an array of BoxReferences into an array of encoding data
 * maps.
 */
export declare function boxReferencesToEncodingData(references: ReadonlyArray<BoxReference>, foreignApps: ReadonlyArray<number | bigint>, appIndex: number | bigint): Array<Map<string, unknown>>;
